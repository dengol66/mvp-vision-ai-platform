[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.huggingface"

[deploy]
startCommand = "python /workspace/training/api_server.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
