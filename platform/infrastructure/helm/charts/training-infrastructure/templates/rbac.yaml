{{- if .Values.rbac.create -}}
# Role for training jobs - minimal permissions
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "training-infrastructure.fullname" . }}-job-role
  namespace: {{ include "training-infrastructure.namespace" . }}
  labels:
    {{- include "training-infrastructure.labels" . | nindent 4 }}
rules:
  # Training jobs need to read configmaps for configuration
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list"]
  # Training jobs need to read secrets for credentials
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
  # Allow jobs to emit events for status reporting
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "training-infrastructure.fullname" . }}-job-rolebinding
  namespace: {{ include "training-infrastructure.namespace" . }}
  labels:
    {{- include "training-infrastructure.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "training-infrastructure.fullname" . }}-job-role
subjects:
  - kind: ServiceAccount
    name: {{ include "training-infrastructure.serviceAccountName" . }}
    namespace: {{ include "training-infrastructure.namespace" . }}
{{- end }}
